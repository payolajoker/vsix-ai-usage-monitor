{
  "name": "ai-usage-statusbar",
  "displayName": "AI Usage Monitor",
  "description": "Displays real-time Claude and Codex usage in the VS Code status bar",
  "icon": "icon.png",
  "version": "0.2.7",
  "publisher": "PayolaJoker",
  "author": "Payola Joker",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/payolajoker/vsix-ai-usage-monitor.git"
  },
  "bugs": {
    "url": "https://github.com/payolajoker/vsix-ai-usage-monitor/issues"
  },
  "homepage": "https://github.com/payolajoker/vsix-ai-usage-monitor#readme",
  "engines": {
    "vscode": "^1.79.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "AI Usage Monitor",
      "properties": {
        "aiUsageMonitor.enabledProviders": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "claude",
              "codex"
            ]
          },
          "uniqueItems": true,
          "default": [
            "claude",
            "codex"
          ],
          "description": "Providers to enable in status bar and refresh cycle. Set empty array to disable all providers."
        },
        "aiUsageMonitor.enableThresholdColors": {
          "type": "boolean",
          "default": true,
          "description": "Enable warning/critical threshold coloring in the status bar."
        },
        "aiUsageMonitor.showProviderLetter": {
          "type": "boolean",
          "default": true,
          "description": "Show provider letter (C/O) next to each provider marker in the status bar."
        },
        "aiUsageMonitor.providerMarkers": {
          "type": "object",
          "default": {
            "claude": "🟠",
            "codex": "🔵"
          },
          "description": "Provider-to-marker mapping used in the status bar. Example: { \"claude\": \"🟠\", \"codex\": \"🔵\" }"
        },
        "aiUsageMonitor.weeklyExhaustedDisplay": {
          "type": "string",
          "enum": [
            "percent",
            "remainingDays"
          ],
          "default": "percent",
          "description": "When weekly usage is exhausted, show either 100% or days remaining until reset in the status bar."
        },
        "aiUsageMonitor.warningThreshold": {
          "type": "number",
          "default": 70,
          "minimum": 0,
          "maximum": 100,
          "description": "Usage percentage threshold where the status bar switches to warning color."
        },
        "aiUsageMonitor.criticalThreshold": {
          "type": "number",
          "default": 85,
          "minimum": 0,
          "maximum": 100,
          "description": "Usage percentage threshold where the status bar switches to critical color."
        },
        "aiUsageMonitor.statusBarColors": {
          "type": "object",
          "default": {
            "disabled": "#6e7681",
            "warning": "#d29922",
            "critical": "#f85149"
          },
          "description": "Status bar color mapping. Supported keys: disabled, warning, critical."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package:vsix": "npm run compile && npx @vscode/vsce package",
    "probe:codex-web": "node tools/codex-rate-limit-check-server.mjs",
    "test:tray": "npm --prefix tray-app test",
    "release:check": "node scripts/release-gate.mjs"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.79.0",
    "@vscode/vsce": "^3.7.1",
    "typescript": "^5.0.0"
  }
}
